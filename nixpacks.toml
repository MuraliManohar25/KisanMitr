[phases.install]
dependsOn = ['setup']
cmds = ['npm install']
paths = ['/app/node_modules/.bin']

[phases.build]
dependsOn = ['install']
cmds = ['npm run build']

[start]
cmd = './node_modules/.bin/serve -s dist -l $PORT'